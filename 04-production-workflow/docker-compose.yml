version: "3"

services:
  web-app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:8080"
    volumes:
      - /go.sum # Don't map this to pwd
      - .:/app # Map current folder to /app inside the container
